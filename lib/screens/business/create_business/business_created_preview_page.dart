import 'package:flutter/material.dart';

class BusinessCreatedPreviewPage extends StatelessWidget {
  const BusinessCreatedPreviewPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
