import 'package:flutter/material.dart';

class ChooseSubscriptionPage extends StatelessWidget {
  const ChooseSubscriptionPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
