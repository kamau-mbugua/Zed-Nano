import 'package:flutter/material.dart';

class ListPaymentsPage extends StatelessWidget {
  const ListPaymentsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
